{
  "name": "my-portfolio-api",
  "version": "1.0.0",
  "description": "Simple API for portfolio contact form",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "tunnel": "npx localtunnel --port 3000"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "@sendgrid/mail": "*",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0"
    ,"localtunnel": "^2.0.2"
  }
}
